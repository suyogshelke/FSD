# FSD

This repository contains assignment folders from Ass1 to Ass10.

## Setup

1. Install dependencies:
   npm install
2. Copy .env.example to .env and update the MongoDB URI.
3. Start the app:
   npm start

If you are using a local MongoDB instance, the default URI is:
`mongodb://127.0.0.1:27017/fsd_assignments`
